{
  "name": "oligarchy",
  "version": "1.0.0",
  "description": "A game about how money always trickles up",
  "main": "src/main.ts",
  "scripts": {
    "dev": "npx nodemon --watch",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stefan Christou",
  "license": "UNLICENSED",
  "devDependencies": {
    "@ngneat/falso": "^6.4.0",
    "@types/node": "^18.15.11",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
